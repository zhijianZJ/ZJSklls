id: issue-api-auth-w01
schema_version: 1.0.0
content_version: 1
status: draft
source: learner report and sanitized API error
confidence: medium
created_at: "2026-07-19T10:00:00+08:00"
updated_at: "2026-07-19T10:00:00+08:00"
user_words: The API keeps returning 401 and I do not know what the English error means.
context:
  current_goal: Complete one authenticated API request for the current practice task.
  current_task: Run the starter request from the local development environment.
issue_type: tool_environment
observed_symptom: The service returns HTTP 401 before the expected response body is produced.
suspected_causes:
  - cause: The execution environment may not contain the credential variable expected by the starter request.
    source: inference from sanitized 401 response
    confidence: medium
earliest_blocker: The request cannot be authenticated, so response parsing and later task steps cannot start.
routing_mode: direct_action
next_action: Check whether the expected credential variable name exists in the same execution environment without printing or sharing its value.
success_signal: The environment confirms that the expected variable name is present while its value remains hidden.
fallback_action: If the variable name is present, share only the sanitized authentication method and error text so the next check can inspect request configuration without exposing the credential.
resolution_status: testing
result_observation: No result has been reported yet.
evidence_ids: []
plan_impact:
  level: none
  reason: One authentication boundary check can be completed without changing the active learning task or plan.
  affected_artifacts: []
  rollback_target: none
next_check: Ask the learner whether the expected variable name was present and what sanitized error remained.
extensions:
  privacy_rule: Never request or persist the credential value.
