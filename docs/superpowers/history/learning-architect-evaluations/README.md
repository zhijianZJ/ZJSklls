# Historical Evaluation Archive

These reports preserve evaluator inputs and outputs from the pre-2.0 runtime. They intentionally retain the former technical identifier because the recorded response blocks and provenance are immutable evidence.

They are not current installation, invocation, or test instructions. Current runtime and tests live under `zjskills/` and `tests/zjskills/`.
